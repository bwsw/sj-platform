sj-benchmark.performance {
  message {
    sizes = ${?MESSAGE_SIZES}
    count = 10000000
    count = ${?MESSAGES_COUNT}
  }

  kafka {
    address = ${?KAFKA_ADDRESS}
    topic = "performance-benchmark-test"
    topic = ${?KAFKA_TOPIC}
  }

  output-file = "benchmark-output-file"
  output-file = ${?OUTPUT_FILE}

  words = "lorem,ipsum,dolor,sit,amet"
  words = ${?WORDS}
}