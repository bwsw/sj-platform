{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Service",
  "type": "object",
  "properties": {
    "name": {
      "description": "The unique name of a service",
      "type": "string"
    },
    "description": {
      "description": "The description of a service",
      "type": "string"
    },
    "type": {
      "description": "The service type",
      "type": "string"
    },
    "namespace": {
      "description": "The service namespace if the service is zookeeper or aerospike type",
      "type": "string"
    },
    "provider": {
      "description": "The provider of service",
      "type": "string"
    },
    "keyspace": {
      "description": "The service keyspace if the service is cassandra type",
      "type": "string"
    },
    "index": {
      "description": "The service index if the service is elasticsearch type",
      "type": "string"
    },
    "login": {
      "description": "The service login if the service is elasticsearch type",
      "type": "string"
    },
    "password": {
      "description": "The service password if the service is elasticsearch type",
      "type": "string"
    },
    "zkNamespace": {
      "description": "A zookeeper namespace if the service is kafka type",
      "type": "string"
    },
    "zkProvider": {
      "description": "A zookeeper provider if the service is kafka type",
      "type": "string"
    },
    "prefix": {
      "description": "A znode path if the service is tstream type",
      "type": "string"
    },
    "token": {
      "description": "A unique key to connect to server if the service is tstream type",
      "type": "string"
    },
    "driver": {
      "description": "A JDBC driver if the service is JDBC type",
      "type": "string"
    },
    "database": {
      "description": "A database name if the service is JDBC type",
      "type": "string"
    },
    "basePath": {
      "description": "A base path if the service is REST type",
      "type": "string"
    },
    "httpVersion": {
      "description": "A version of HTTP if the service is REST type",
      "type": "string"
    },
    "headers": {
      "description": "A HTTP headers if the service is REST type",
      "type": "object"
    }
  }
}