{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Service",
  "type": "object",
  "properties": {
    "name": {
      "description": "The unique name of a service",
      "type": "string"
    },
    "description": {
      "description": "The description of a service",
      "type": "string"
    },
    "type": {
      "description": "The service type",
      "type": "string"
    },
    "namespace": {
      "description": "The service namespace if the service is zookeeper or aerospike type",
      "type": "string"
    },
    "provider": {
      "description": "The provider of service if the service is zookeeper/aerospike/elasticsearch/kafka/JDBC type",
      "type": "string"
    },
    "keyspace": {
      "description": "The service keyspace if the service is cassandra type",
      "type": "string"
    },
    "index": {
      "description": "The service index if the service is elasticsearch type",
      "type": "string"
    },
    "login": {
      "description": "The service login if the service is elasticsearch type",
      "type": "string"
    },
    "password": {
      "description": "The service password if the service is elasticsearch type",
      "type": "string"
    },
    "zk-namespace": {
      "description": "A zookeeper namespace if the service is kafka type",
      "type": "string"
    },
    "zk-provider": {
      "description": "A zookeeper provider if the service is kafka type",
      "type": "string"
    },
    "metadata-namespace": {
      "description": "A cassandra namespace if the service is tstream type",
      "type": "string"
    },
    "metadata-provider": {
      "description": "A cassandra provider if the service is tstream type",
      "type": "string"
    },
    "data-namespace": {
      "description": "A cassandra or aerospike namespace if the service is tstream type",
      "type": "string"
    },
    "data-provider": {
      "description": "A cassandra or aerospike provider if the service is tstream type",
      "type": "string"
    },
    "lock-namespace": {
      "description": "A zookeeper namespace if the service is tstream type",
      "type": "string"
    },
    "lock-provider": {
      "description": "A zookeeper provider if the service is tstream type",
      "type": "string"
    },
    "driver": {
      "description": "A JDBC driver if the service is JDBC type",
      "type": "string"
    },
    "database": {
      "description": "A database name if the service is JDBC type",
      "type": "string"
    }
  }
}