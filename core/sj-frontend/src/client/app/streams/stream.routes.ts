import { RouterConfig } from '@angular/router';

import { StreamsComponent } from './index';

export const StreamsRoutes: RouterConfig = [
    {
        path: 'streams',
        component: StreamsComponent
    },
];

