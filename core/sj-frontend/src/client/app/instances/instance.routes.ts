import { RouterConfig } from '@angular/router';

import { InstancesComponent } from './index';

export const InstancesRoutes: RouterConfig = [
    {
        path: 'instances',
        component: InstancesComponent
    },
];

